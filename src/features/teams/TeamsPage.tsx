// Story 2.4 — Команда: создание + join по коду
// TODO: create team (generates join code), join by code flow

export default function TeamsPage() {
  return (
    <main className="p-6">
      <h1 className="text-2xl font-bold">Команды</h1>
      <p className="mt-2 text-gray-500">TODO: создать команду / присоединиться по коду</p>
    </main>
  );
}
